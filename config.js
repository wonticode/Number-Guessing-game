// to make the code look more clean, and easily editable config

// game stuff
export const GAME_CONFIG = {
  MIN_NUMBER: 1,
  MAX_NUMBER: 100,
  MAX_ATTEMPTS: 5,
  SLIGHT_MARGIN: 10,
};

// element ids stuff
export const UI_IDS = {
  input: "guessNum",
  checkBtn: "checkNum",
  resetBtn: "reset",
  resultText: "resultDesc",
  attemptsText: "attempts",
};

// css stuff
export const CSS_CLASSES = {
  shakeEffect: "shake",
};
